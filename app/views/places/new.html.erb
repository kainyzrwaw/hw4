<h1 class="py-4">New Place</h1>

<% if @current_user %>

<%= form_with :model => @place do |form| %>
  <p>
    <%= form.label "name" %>
    <%= form.text_field "name" %>
  </p>
  <p class="btn btn-primary">
    <%= form.submit %>
  </p>
<% end %>

<% end %>